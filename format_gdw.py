from setup_gloric import *

gdw_gdb = os.path.join(datdir, ant)
gdw_pt =
gdw_poly =

up_area = os.path.join(datdir, 'hydroatlas', 'upstream_area_skm_15s.gdb', 'up_area_skm_15s')

gdw_processgdb =



#removed planned and in construction dams
#remove -99 and -9999 values
#Long_riv Lat_riv: make sure they match the actual x and y
#Extract upstream area to make sure it matches

#Every 5 years
#Accumulate storage value downstream
#Compute DOR
#Expand to reservoirs

